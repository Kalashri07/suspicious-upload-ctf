version: '3.8'

services:
  ctf-challenge:
    build: .
    container_name: suspicious-upload-ctf
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
